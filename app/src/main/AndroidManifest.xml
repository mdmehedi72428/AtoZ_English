<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:andriod="http://schemas.android.com/apk/distribution"
    package="com.example.customadapter">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GirdViewMainActivity"></activity>
        <activity android:name=".A_Teacher" />
        <activity android:name=".May_Day" />
        <activity android:name=".Deforestration" />
        <activity android:name=".Traffic_Jam" />
        <activity
            android:name=".a_favourite_teacher"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Winter_Moring"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".A_Rainy_Day"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>